@page
@model Seashore_CRM.Pages.Contacts.IndexModel
@{
    ViewData["Title"] = "Contacts";
}

<h1>Contacts</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">+ New Contact</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Company</th>
            <th>Phone</th>
            <th>Email</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var c in Model.Contacts)
    {
        <tr>
            <td>@(c.FirstName + " " + c.LastName)</td>
            <td>@c.Company?.CompanyName</td>
            <td>@c.Phone</td>
            <td>@c.Email</td>
            <td>
                <a asp-page="Details" asp-route-id="@c.Id">Details</a> |
                <a asp-page="Edit" asp-route-id="@c.Id">Edit</a> |
                <a asp-page="Delete" asp-route-id="@c.Id">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>